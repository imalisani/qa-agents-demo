{
  "name": "agentic-quality-engineering-demo",
  "version": "1.0.0",
  "private": true,
  "description": "A portfolio-ready Agentic Quality Engineering experiment using specialized QA agents, reusable skills, and Playwright.",
  "type": "module",
  "scripts": {
    "start": "node app/server.mjs",
    "pretest": "node scripts/prepare-test-results.mjs",
    "test": "playwright test --project=chromium",
    "test:unit": "node --test --experimental-test-coverage --test-coverage-include=app/refund-store.mjs --test-coverage-branches=90 unit-tests/*.test.mjs",
    "test:deploy": "playwright test --config=playwright.deploy.config.ts",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report reports/playwright-html",
    "test:allure": "npm test && npm run allure:generate",
    "test:portfolio": "node -e \"require('fs').rmSync('allure-results/portfolio',{recursive:true,force:true})\" && cross-env ALLURE_RESULTS_DIR=allure-results/portfolio playwright test --project=portfolio-showcase && allure generate allure-results/portfolio --clean -o reports/allure-portfolio",
    "allure:generate": "allure generate allure-results/current --clean -o reports/allure-current && node scripts/prepare-report.mjs",
    "allure:open": "allure open reports/allure-current",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "validate": "npm run lint && npm run typecheck && npm run test:unit && npm test"
  },
  "engines": { "node": ">=24" },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@eslint/js": "^9.0.0",
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.0.0",
    "allure-commandline": "^2.43.0",
    "allure-playwright": "^3.11.0",
    "cross-env": "^10.1.0",
    "eslint": "^9.0.0",
    "globals": "^16.0.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.0.0"
  }
}
